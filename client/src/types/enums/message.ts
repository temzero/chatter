export enum MessageStatus {
  SENT = "sent",
  DELIVERED = "delivered",
  READ = "read",
  EDITED = "edited",
  DELETED = "deleted",
}

export enum MessageType {
  TEXT = 'text',
  IMAGE = 'image',
  VIDEO = 'video',
  AUDIO = 'audio',
  FILE = 'file',
  LOCATION = 'location',
  POLL = 'poll',
  SYSTEM = 'system',
  FORWARD = 'forward',
}

