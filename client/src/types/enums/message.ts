export enum MessageStatus {
  SENT = "sent",
  SENDING = "sending",
  DELIVERED = "delivered",
  READ = "read",
  EDITED = "edited",
  DELETED = "deleted",
  FAILED = "failed",
}

export enum MessageType {
  TEXT = 'text',
  IMAGE = 'image',
  VIDEO = 'video',
  AUDIO = 'audio',
  FILE = 'file',
  LOCATION = 'location',
  POLL = 'poll',
  SYSTEM = 'system',
  FORWARD = 'forward',
}

