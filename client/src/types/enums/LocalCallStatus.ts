export enum LocalCallStatus {
  CONNECTING = "connecting",
  CONNECTED = "connected",
  INCOMING = "incoming",
  OUTGOING = "outgoing",
  ONGOING = "ongoing",
  CANCELED = "canceled",
  REJECTED = "rejected",
  ENDED = "ended",
  ERROR = "error",
}
