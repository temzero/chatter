export enum LocalCallStatus {
  CONNECTING = "connecting",
  CONNECTED = "connected",
  INCOMING = "incoming",
  OUTGOING = "outgoing",
  CHECK_BROADCAST = "check_broadcast",
  TIMEOUT = "timeout",
  CANCELED = "canceled",
  DECLINED = "declined",
  ENDED = "ended",
  ERROR = "error",
}
