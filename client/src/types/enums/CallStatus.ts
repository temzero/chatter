export enum CallStatus {
  CONNECTING = "connecting",
  CONNECTED = "connected",
  INCOMING = "incoming",
  OUTGOING = "outgoing",
  CANCELED = "canceled",
  REJECTED = "rejected",
  ENDED = "ended",
  ERROR = "error",
}
